{
  "name": "carassembly",
  "version": "1.0.0",
  "description": "",
  "main": "server/app.js",
  "scripts": {
    "install": "cd client && npm install && cd ../server && npm install && cd ..",
    "build": "cd client && npm run build && cd ..",
    "start": "node server/app.js",
    "test": "cd server && npm install -g jest && npm run test",
    "customTest": "cd server && npm install -g jest && npm run customTest && npm run customTest --no-color &> ../output.txt",
    "heroku-prebuild": "cd client && npm install -g @vue/cli-service @vue/cli-plugin-babel @vue/cli-plugin-eslint && cd .."
  },
  "author": "Tamerlan",
  "license": "ISC",
  "dependencies": {
    "carassembly-client": "file:client",
    "carassembly-server": "file:server"
  },
  "engines": {
    "node": "14"
  }
}
